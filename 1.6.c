#include<stdio.h>
int main(){
    // int arr[50] = {1,2,3,4,5,[47]=53,54,68}; // // when value of a[i] is not assigned it gives 0 .
    // for (int i = 0; i < 50; i++)
    // {
    //     printf("%d ",arr[i]);
    // }    
    // int arr[2] = {34, 54, 6, 4}; // // only 2 initialised so output gives garbage value in cpp gives error.
    // for (int i = 0; i < 4; i++)
    // {
    //     printf("%d ",arr[i]);
    // }
    
    return 0;
}